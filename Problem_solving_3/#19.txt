#include <iostream>
#include <string>
#include "MyLib.h"
#include<random>
#include<iomanip>
#include<vector>
using namespace std;


void FillArrayWithNumbersFrom0To1(int Matrix[3][3], short Rows, short Colm) {
	
	for (int f = 0; f < Rows; f++) {
		for (int g = 0; g < Colm; g++) {
			Matrix[f][g] = MyLib::RandomNumber(0,100);
		}
	}
	
}

void PrintMatrixElement(int Matrix[3][3], short Rows, short Colm) {
	
	for (int a = 0; a < Rows; a++) {
		for (int x = 0; x < Colm; x++) {
			cout<<setw(3)<<Matrix[a][x]<<"  ";
		}
		cout << endl;
	}

}
void GetMaxAndMinNumberInMatrix(int Matrix[3][3], short Rows, short Colm, short &Max, short &Min) {
	 Min = Matrix[0][0];
	 Max = Matrix[0][0];
	for (int a = 0; a < Rows; a++) {
		for (int x = 0; x < Colm; x++) {
			if (Matrix[a][x] > Max) {
				Max = Matrix[a][x];
			}
			else if (Matrix[a][x] < Min) {
				Min = Matrix[a][x];
			}
		}
	}
	
}



int main(){

	srand((unsigned)time(NULL));
	int Mtx1[3][3];
	short Max = 0, Min = 0;
	
	cout << "Matrix 1 :\n";

	FillArrayWithNumbersFrom0To1(Mtx1, 3, 3);
	PrintMatrixElement(Mtx1, 3, 3);
	GetMaxAndMinNumberInMatrix(Mtx1, 3, 3, Max, Min);
	cout << "Minmum Number is : " << Min << endl;
	cout << "Maximum Number is : " << Max << endl;



	system("pause>0");
}

